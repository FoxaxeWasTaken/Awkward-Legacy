services:
  client-dev:
    build:
      context: ./client
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
    volumes:
      - ./client/:/app
      - /app/node_modules
    command: ["npm", "run", "dev"]
  
  server-dev:
    build:
      context: ./server
      dockerfile: Dockerfile.dev
    ports:
      - "8000:8000"
    environment:
      - NODE_ENV=development
    volumes:
      - ./server/src:/app/src
      - ./server/tests:/app/tests
      - /app/node_modules
