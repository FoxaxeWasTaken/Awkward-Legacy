services:
  client-dev:
    build:
      context: ./client/geneweb_client
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
    volumes:
      - ./client/geneweb_client/:/app
      - /app/node_modules
    command: ["npm", "run", "dev"]
